# DeepSeek 费用与限额

## 默认预算

演示阶段默认使用 `deepseek-flash`，所有重试均计入预算。

- 单任务：0.80 元预警，1.00 元硬停止；最多输入 250,000 tokens、输出 50,000 tokens、20 次模型调用；每步骤最多重试 2 次。
- 每日：8.00 元预警，10.00 元硬停止；最多输入 2,500,000 tokens、输出 500,000 tokens。
- 成本一律按高峰、输入缓存未命中的最坏情形预占；实际扣费可能更低，但不能用可能的缓存命中提前透支预算。
- 达到上限时不启动下一阶段，保留已完成结果并明确标记“预算停止”；不能静默生成残缺报告。
- 只有 429、500、502、503、504 和网络瞬时错误允许有限重试；鉴权、参数、权限和内容错误不重试。

机器可读配置位于 `config/deepseek-budget.json`，校验程序为 `tests/validate_deepseek_budget.py`。

## 价格快照

核对日期：2026-09-19。官方 `deepseek-flash` 人民币价格按百万 tokens 计：

- 闲时：缓存命中输入 0.02 元、缓存未命中输入 1 元、输出 4 元。
- 高峰：缓存命中输入 0.04 元、缓存未命中输入 2 元、输出 8 元。
- 北京时间周一至周五 09:00–12:00、14:00–18:00 为高峰，其余为闲时。

官方价格可能变化。程序必须记录价格版本、输入／输出／缓存命中 tokens 和估算费用；账单后台扣费是最终依据。非紧急批量评测优先安排在闲时。

官方依据：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/

## 已有真实用量校准

- 全部提示词迭代：151 次调用，输入 337,047 tokens，输出 99,947 tokens，总计 436,994 tokens。按全部输入缓存未命中估算，闲时约 0.737 元，高峰约 1.474 元。
- 最终放行轮：57 次调用，输入 128,211 tokens，输出 34,554 tokens，总计 162,765 tokens。按相同口径估算，闲时约 0.266 元，高峰约 0.533 元。

这些数字是固定测试集的实测，不等于单个真实用户任务的典型成本。上线后要分别记录检索、生成、核验和重试成本，再根据真实分布调整预算。

## 执行规则

1. 创建任务时读取当日累计用量；预算不足则不启动。
2. 每次调用前按最大可能输出预留预算，响应后用真实 usage 回冲。
3. 任务内部按术语扩展、证据筛选、摘要生成、引用核验分别计账。
4. 输入证据优先裁剪、去重和缓存；不得为省钱删掉支持关键结论的原始证据。
5. 达到 80% 时在创建者后台预警；达到硬上限时停止后续调用。
6. 管理员可以为单个任务提高上限，但必须记录操作者、原因、旧值和新值。
7. 价格表更新或模型切换后必须重新运行预算校验，不沿用旧成本估算。

## 验收边界

本次完成预算规则、价格快照、已有真实用量校准和配置一致性测试。当前未读取 DeepSeek 账户余额；余额属于账户状态，应由创建者后台或服务端安全检查，不能写入仓库或聊天。

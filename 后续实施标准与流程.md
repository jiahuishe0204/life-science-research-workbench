# 后续实施标准与流程

更新日期：2026-09-23

## 完成的统一定义

每项工作依次经过“代码完成 → 自动测试通过 → 已部署 → 线上真实验证”。只有最后一步通过，状态才可以写成“完成”。书面方案、本地测试或平台显示部署成功，都不能单独作为完成证据。

每项线上验证必须保存：部署环境、时间、测试输入、结果摘要、失败或降级路径、费用、证据文件位置。账号密钥、密码、TOTP 秘钥、Cookie、真实用户输入和备份不得进入证据文件或 Git。

## 自动推进规则

以下工作由开发流程直接继续，不逐项等待确认：仓库内可逆修改、自动测试、只读诊断、文档与验收证据更新、已授权私有仓库推送。

遇到以下事项暂停并请求用户操作：输入或生成真实密钥、外部账号审批、购买或付费、绑定或购买域名、公开发布、删除真实数据、覆盖或恢复生产数据。

失败时不得反复新建任务。优先恢复原任务；外部接口不可用时记录真实错误和降级通道。单次调研模型费用达到 1 元立即停止。

## 执行顺序

1. 完成 EdgeOne 新版的完整报告、隔离和取消复验。当前代码和部署已验证到论文检索阶段，等待新的限时访问地址恢复原任务。
2. 实现创建者密码、TOTP、会话、CSRF、限速和审计事件，并分别做本地与线上未授权／授权测试。
3. 实现创建者统计、任务导出和带 SHA-256 清单的备份包。
4. 在隔离命名空间完成一次恢复演练，不覆盖生产数据。
5. 把已定稿的官方来源白名单接入真实任务链，验证允许、拒绝和跳转 URL。
6. EPO 批准参考编号 45022 后，创建应用凭据并完成 OAuth、检索、详情、限流和异常实测。
7. 用至少五个生命科学主题做生产端到端验收，覆盖引用、权限、费用、失败、移动端和 HTTPS。
8. 所有验收通过后，再由创建者决定是否公开发布及是否绑定自定义域名。

机器可读进度和逐项通过条件位于 `config/implementation-roadmap.json`。

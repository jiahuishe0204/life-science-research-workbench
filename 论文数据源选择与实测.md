# 论文数据源选择与实测

## 1. 最终决定

首版采用三层组合，不把三个来源当作同等用途的并列搜索引擎：

- **PubMed：主检索源。** 用于生命科学论文发现、主题检索和候选集生成。
- **Europe PMC：全文与开放获取补充源。** 使用 PMID 或 DOI 补充开放获取状态、PMC/Europe PMC 全文、引用和扩展元数据；必要时用于补充预印本候选，但预印本必须单独标记，不能与同行评议论文混排为同等证据。
- **Crossref：DOI 身份核对源。** 用于 DOI 规范化、题名／作者／期刊／年份补全和跨源去重；不作为首版生命科学主题检索主源。

身份合并顺序为：规范化 DOI > PMID > 规范化题名 + 年份。合并不能覆盖来源差异；每条证据保留原来源、原始标识符和原始链接。

## 2. 真实测试方法

测试日期：2026-09-19。

测试主题覆盖基因编辑、空间组学、代谢药物、神经退行性疾病标志物和 CAR-T 实体瘤，共 5 个主题。每个来源每个主题取相关性排序前 20 条，日期范围固定为 2022-01-01 至 2026-09-19。

测试记录：

- 固定查询：`tests/fixtures/paper-source-queries.json`
- 可重复运行程序：`tests/run_paper_source_eval.py`
- 最终原始响应与汇总：`artifacts/paper-source-eval/20260919T083041Z/`

三方查询语法和收录范围不同，总命中数不能直接横向比较。测试重点是：五类主题能否检出、前 20 条的主题相关性、关键字段完整度、全文线索、重复项、响应时间和可追溯链接。

## 3. 实测结果

### PubMed

- 5/5 个主题均成功；共返回 100 条。
- DOI、摘要、作者、年份、期刊和原始链接均为 100/100。
- 中位响应时间 4.908 秒；一次查询包含搜索和批量详情读取两个请求。
- 前列标题与五个测试主题匹配度稳定。
- 本轮结果中出现 1 个重复 DOI，说明生产流程仍必须按 DOI 去重。
- PubMed 本身不提供统一的开放获取／全文可用标志，需要 Europe PMC 或 PMC 补充。

### Europe PMC

- 5/5 个主题均成功；共返回 100 条。
- DOI 96/100、摘要 87/100、作者 98/100、年份 98/100、期刊 94/100、来源链接 100/100。
- 45/100 标记为开放获取，52/100 存在 PMC 或 Europe PMC 全文信号。
- 中位响应时间 3.368 秒。
- 覆盖范围明显更宽，适合补充全文、开放获取和扩展记录；也因此必须区分 PubMed 记录、预印本及其他来源类型。

### Crossref

- 5/5 个主题均成功；共返回 100 条。
- DOI、年份、期刊和 DOI 链接均为 100/100；作者 87/100，摘要仅 42/100。
- 中位响应时间 3.153 秒。
- 主题检索的候选范围很宽，前列结果会混入评论、新闻、勘误或质量不一的出版物；不适合单独承担生命科学证据发现。
- DOI 和基础书目信息完整，适合作为身份核对与元数据补全服务。

### 排序差异

三个来源的前 20 条 DOI 重合很低。这不表示记录互不收录，而是反映检索字段、语法、数据范围和相关性排序不同。生产流程不能按排名位置合并，应先保存各源标识符，再按 DOI／PMID 做身份合并。

## 4. 接入规则

1. PubMed 查询使用逐词 `AND` 的 Title/Abstract 条件；高级任务再增加 MeSH、文章类型和日期过滤，避免把整句误当作精确短语。
2. PubMed 先搜索 PMID，再批量读取详情；无 API Key 时不超过每秒 3 个请求。生产环境应配置 `tool`、开发者联系邮箱，并优先使用批量接口和缓存。
3. Europe PMC 使用 `resultType=core`；全文仅在 `inEPMC=Y` 或 `inPMC=Y` 时记为平台全文，不能把普通 DOI 跳转误记为全文。
4. Crossref 使用精确 DOI 路径做身份核对；主题查询只作为兜底。根据响应中的限流头动态节流，本轮公共池实际返回每秒 1 次和并发 1 的限制。
5. 三个来源都实施有限重试、指数退避、超时、缓存和 429/5xx 处理；一次来源失败不允许静默改写为“无结果”。
6. 入选证据仍受总上限 19 条约束；候选集可以更大，但必须记录筛选版本和淘汰原因。
7. 页面显示 PMID、DOI、来源、出版年份、原始链接、全文／摘要边界和预印本状态。

## 5. 官方依据

- NCBI E-utilities：https://www.ncbi.nlm.nih.gov/home/develop/api/
- NCBI 请求规范：https://www.ncbi.nlm.nih.gov/home/about/policies/
- Europe PMC REST API：https://europepmc.org/RestfulWebService
- Crossref REST API：https://www.crossref.org/documentation/retrieve-metadata/rest-api/
- Crossref 访问与限流：https://www.crossref.org/documentation/retrieve-metadata/rest-api/access-and-authentication/

## 6. 结论边界

本次测试证明三个公开接口在当前网络环境中可以真实访问，并完成了五类主题的小规模字段和检索对比；它不是大规模召回率研究。正式网站上线后仍需用真实用户主题持续记录零结果率、人工相关性、字段缺失、429/5xx、延迟和跨源合并错误，并按版本复测。

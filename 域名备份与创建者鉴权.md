# 域名、备份与创建者鉴权定稿

定稿日期：2026-09-20  
适用范围：三天 Demo 与首次小范围公开试用

## 结论

本项的规则、机器配置和本地校验已经完成；尚未把它误记为 EdgeOne 真实部署完成。部署后仍需验证平台域名、HTTPS、存储、导出、恢复和登录流程。

## 1. 域名与网络

- Demo 首次部署使用 EdgeOne Makers 平台域名。首页、普通接口和创建者接口保持同源，默认不开放跨域。
- 平台部署、接口连通和 HTTPS 验证通过后再绑定自定义域名，避免域名问题与应用问题同时排查。
- 未完成备案时，只选择“全球可用区（不含中国大陆）”；需要中国大陆或全球可用区时，先完成备案。未做真实网络测试前，不宣传“内地稳定”。
- 自定义域名必须启用 HTTPS，HTTP 跳转 HTTPS；HSTS 仅在证书、跳转和回退均验证后开启。
- DeepSeek、EPO、管理员和任务访问密钥只放服务端环境变量，不进入浏览器代码、Git、日志或统计事件。

## 2. 数据保存与备份

- 候选实现使用 EdgeOne Blob 保存报告 JSON、Markdown 和导出文件；KV 只保存小型配置、计数器或短状态。平台存储必须经过真实部署测试后才能标为可用。
- 用户任务、原始输入、检索结果和报告默认保留 7 天；安全审计事件保留 30 天；不含原始用户输入的汇总指标保留 90 天。
- 每日以及数据结构变更后生成加密备份，保存于 Git 之外、由创建者控制的位置。保留 7 份日备份和 4 份周备份。
- 每份备份含记录数、文件清单和 SHA-256 校验值；发布前做一次恢复演练，此后每月一次。
- 用户数据删除必须覆盖仍在保留期内的备份；任何真实用户数据、数据库或备份均不得提交 GitHub。
- EdgeOne Makers 的 KV 与 Blob 文档均提示一致性边界，因此应用必须提供导出副本，不能只依赖平台内的一份状态。

## 3. 创建者后台

- 仅有一个创建者账号，不提供公开注册、找回密码或普通用户登录入口；后台路径为 `/creator`，后台接口为 `/api/creator/*`，默认拒绝未登录请求。
- 密码只保存 Argon2id 哈希；公开试用前必须启用 TOTP 二次验证。真实哈希、会话签名密钥和 TOTP 秘钥仅写入服务端环境变量。
- 管理会话 Cookie 使用 `Secure`、`HttpOnly`、`SameSite=Strict`；空闲 30 分钟失效，最长 8 小时，登录成功后轮换会话标识。
- 所有改变状态的后台请求校验 CSRF；15 分钟内最多失败 5 次，按账号和 IP 限速并锁定 15 分钟。
- 记录登录成功／失败、退出、数据导出和配置变更，但不记录密码、验证码、会话值、API Key 或完整用户输入。

## 4. 匿名任务访问

- 普通用户仍可匿名试用，但任务编号不是授权凭据。
- 创建任务时生成至少 128 位随机访问令牌，通过 `Secure`、`HttpOnly` Cookie 保存；读取、取消、反馈和导出均同时校验任务与访问令牌。
- 页面与接口不得暴露可枚举的连续任务编号；运营统计只保留去标识化汇总。

## 5. 部署验收

只有以下项目均通过后，才能把本项从“规则完成”提升为“部署验证完成”：

1. 平台域名可访问，HTTP 自动跳转 HTTPS，证书链正确。
2. 浏览器包和构建日志中不存在服务端密钥，跨域请求默认失败。
3. 未登录不能访问创建者页面或接口；口令、TOTP、限速、超时和退出均实测通过。
4. 两个匿名会话不能互相读取、取消或导出任务。
5. 完成一轮任务保存、导出、校验、删除和恢复演练，并记录容量与耗时。
6. 从部署环境实测 DeepSeek、论文接口和 EPO（凭据恢复后）的连接。

## 6. 机器可读规则

规则位于 `config/security-retention.json`；执行 `python3 tests/validate_security_retention.py` 可检查关键安全下限。该检查只验证配置，不替代部署后的安全测试与恢复演练。

## 官方依据

- EdgeOne Makers 存储概述：https://cloud.tencent.com/document/product/1552/131424
- EdgeOne Makers Blob：https://cloud.tencent.com/document/product/1552/131425
- EdgeOne Makers HTTPS：https://cloud.tencent.com/document/product/1552/127406
- EdgeOne 使用限制与备案边界：https://cloud.tencent.com/document/product/1552/104964

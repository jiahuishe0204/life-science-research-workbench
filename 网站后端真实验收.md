# 网站后端真实验收

验收日期：2026-09-20

## 结论

本地零依赖后端已经接入 DeepSeek、PubMed、Europe PMC 和 Crossref，并完成真实端到端运行。任务创建、匿名会话隔离、防重复标识、状态轮询、取消入口、服务重启恢复、来源保存、引用编号校验、费用上界和七天数据清理均已实现。

EPO OPS 账号仍在等待管理员审批，因此每个任务均明确标记未执行专利检索；本次结果不是包含专利的完整调研。

## 真实运行结果

首轮运行证据：`artifacts/backend-e2e/20260920T090405Z/`

- 5/5 个主题完成报告。
- 每个主题获得 12 条去重来源，同时包含 PubMed 和 Europe PMC。
- 报告引用编号均来自本任务的来源白名单。
- 5 个任务合计模型费用上界估算为 ¥0.3112；每个任务均低于 ¥1 硬停止线。
- 首轮自动验收通过 4/5。唯一失败项为“单细胞测序”报告正文没有重复披露 EPO 缺口，尽管页面任务状态已显示该限制。

修复：程序在最终报告缺少 `EPO` 时强制追加“未执行专利检索、不代表不存在相关专利”的声明。

复测证据：`artifacts/backend-e2e/20260920T090659Z/`

- 失败主题单独复测完成。
- 12 条来源同时覆盖 PubMed 和 Europe PMC。
- 引用编号、专利缺口披露和费用检查全部通过。
- 复测费用上界估算 ¥0.0636。

## 已验证的真实流程

1. 用户只输入中文生命科学主题。
2. DeepSeek 非思考模式生成英文检索式；若超时则使用本地保守词表降级。
3. PubMed 精确检索无结果时自动放宽非核心修饰词。
4. Europe PMC 补充开放获取与全文信号。
5. DOI 去重后，最多 4 条通过 Crossref 逐项核对。
6. DeepSeek 仅基于已读取的摘要级证据生成报告。
7. 第二次模型调用删除或减弱不支持结论，并校验 `[S#]` 只引用本任务来源。
8. 页面展示模型用量、峰值价格费用上界、来源范围和 EPO 缺口。

## 尚未通过的边界

- EPO OPS 真实专利检索：等待账号审批及应用凭据。
- 创建者登录：页面入口已存在，真实 Argon2id/TOTP 服务尚未接入。
- EdgeOne：尚未创建项目、配置环境变量、迁移本地 SQLite 状态或执行 HTTPS／存储／网络验收。
- 当前 Python 后端是本地可运行实现，不能直接等同于 EdgeOne Makers 函数部署完成。

## 本地运行

```bash
python3 server.py
```

浏览器访问 `http://127.0.0.1:4173`。真实密钥只从被 Git 忽略的 `.env` 读取。
